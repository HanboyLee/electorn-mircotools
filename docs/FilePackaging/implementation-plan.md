# 文件打包 · 實現計劃（待設計確認後執行）

> 狀態：**已執行**  
> 前置：用戶已確認設計  
> 範圍：主要 `src/pages/FilePackaging/`；**未改** `ZipService` 掃描規則與 IPC 契約  

---

## 階段 0

- [ ] 對照 `ui-ux-review.md` 驗收標準  
- [ ] 文案繁體；確認不恢復打包歷史  
- [ ] 僅保留「全部打包」一條路徑（循環全部 `fileGroups`）；刪除勾選打包與行內單組打包

---

## 階段 1 · 骨架與目錄卡

- [ ] 頁眉 + 規則一句話  
- [ ] 工作目錄卡：路徑、組數、**選擇目錄** / **打開打包目錄**（無重新掃描）  
- [ ] 就緒檢查組件  
- [ ] 空態 / 無匹配空態

---

## 階段 2 · 文件組一覽表

- [ ] 表格列：組名、擴展名 Tags、`files.length`、可選總大小、狀態  
- [ ] **無** checkbox、**無**行內打包、**無**行內詳情  
- [ ] 搜索過濾僅影響展示；打包仍針對全表數據  
- [ ] `rowKey` 穩定；分頁  

---

## 階段 3 · 全部打包與進度

- [ ] 底欄：輔助文案「將打包全部 N 組」+ 唯一主 CTA「全部打包」  
- [ ] 確認 Modal（全部 N 組）文案重寫  
- [ ] **真實**組進度 `i/m` + 當前組名；移除假 setInterval 進度  
- [ ] 打包中鎖定選目錄 / 再次打包；打開打包目錄仍可用  

---

## 階段 4 · 結果與打磨

- [ ] 內嵌結果摘要 + 列表（無行級打開、無結果區打開目錄）  
- [ ] 「打開打包目錄」僅在工作目錄卡 → `selectedDirectory`  
- [ ] toast 完成統計  
- [ ] 清理 console.log  
- [ ] 主題 token 目測（含 dark）  
- [ ] 可選：`logic/` 抽進度/過濾純函數 + `__tests__`（不入庫）

---

## 非目標

- 改 ZipService 擴展名列表  
- 打包歷史  
- **重新掃描**按鈕  
- 自選輸出目錄  
- 新 UI 庫  

---

## 建議提交信息

```text
feat(file-packaging): redesign file packaging workspace UI
```
